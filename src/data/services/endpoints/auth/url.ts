// All endpoint urls will go here
// Starting with the base endpoints
// And then any extending endpoints

const auth = "/auth";

export const login = `${auth}/login`;
export const logout = `${auth}/logout`;
